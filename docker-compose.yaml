services:
  qt4-kde:
    build: .
    container_name: qt4-kde
    ports:
      - "5901:5901"   # VNC (corregido)
      - "6080:6080"   # noVNC
    restart: unless-stopped
    # Añadido para mejor diagnóstico
    stdin_open: true
    tty: true