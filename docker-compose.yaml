services:
  debian-desktop:
    image: dkge/debian-xfce-vnc:latest
    container_name: debian-xfce2
    ports:
      - "6080:6080"
    environment:
      - VNC_PASSWORD=1234
      - VNC_RESOLUTION=1920x1080
      - VNC_DEPTH=24
    volumes:
      - ./data:/opt/data
    restart: unless-stopped